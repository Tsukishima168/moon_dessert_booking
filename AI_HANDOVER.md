# AI Handover Document (2026-03-10)

## 📍 專案狀態快照

今日已針對前台三大 repo (`moon-dessert-booking`, `color-of-kiwimu-mbti-lab-v5`, `moonmoon-dessert-passport`) 執行基礎設施（Phase 1）的全面配置與覆核。

### 1. 基礎設施與環境變數 (Vercel)
- **GA4 埋點**：三個專案皆已完成 `gtag` 事件埋入 (`add_to_cart`, `begin_checkout`, `quiz_completion`, `passport_checkin`)，對應的 Environment Variables (`NEXT_PUBLIC_GA4_ID`, `VITE_GA4_ID` 等) 已確認存在。
- **Resend 信件配置**：`moon-dessert-booking` 已於 Vercel 正式載入 `RESEND_FROM_EMAIL=noreply@kiwimu.com` 及 `RESEND_FROM_NAME=月島甜點 MOON MOON`。網域認證（DNS）於 Cloudflare 端皆已 verified。
- **LINE Pay 支援預留**：程式實際讀取 `LINEPAY_CHANNEL_ID` / `LINEPAY_CHANNEL_SECRET`，未設定 `LINEPAY_API_URL` 時會預設走 sandbox。

### 2. Repo 與部署狀態
- 五大衍生附屬專案皆已於 GitHub 設定為 **Public**。
- Vercel 上的最新 `--prod` 部署皆成功 (Ready)。各專案 `README.md` 已置頂新增「冷啟動快照 · 2026-03-10」。

### 3. 第三方儀表板現況盤點
- **ManyChat**：目前為 Free 方案（401 Subscribers），LINE 未連線。
- **Supabase**：`moon_map` 專案（主庫）已完成 `orders`, `profiles` 等 17 張主表擴充，狀態良好。
- **n8n**：目前無法連線 `localhost:5678` 或 `n8n.kiwimu.com`。未來接手若需盤點 Workflow，請參考文件紀錄的 SOP 手動查核。

---

## 🚀 明日 (Next Phase) 建議行動清單

1. **LINE Pay 串接與測試**：待金鑰取得後，填入變數並接續開發結帳金流邏輯。
2. **MBTI v2 與 Admin 介面優化**：主分支目前皆為 `production`，另有 `feat/mbti-v2` 與 `feat/admin-polish` 正在進行，可接手繼續推進。
3. **效能優化 (Optional)**：`kiwimu-mbti` 在 Vercel 部署時有 Vite 的建置警告 (動態載入衝突、Bundle size 過大等)，若有空檔也可排入 Code Splitting 優化。
4. **覆核 n8n**：確認 webhook 串接及通知流程是否需要重新部署啟動。

*This document was generated by Antigravity to ensure a seamless context handover to the next AI collaborator.*
